// Notifications, Alerts

// Needs to support a stream of messages
// Consider zustand

// How to delete a message?
// Should timers be handled? - yes

// How to add support for animation?

// const { message, messages } = useMessages();
// const {} = useMessage();


// const id = messages.add({}, timeout);
// messages.remove(id);
// messages.flush();
